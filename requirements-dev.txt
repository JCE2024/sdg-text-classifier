# Entorno de trabajo del notebook (incluye todo lo de la aplicación).
-r requirements.txt

matplotlib==3.10.6
jupyterlab==4.4.7
nbconvert==7.16.6

# Búsqueda de hiperparámetros. keras-tuner importa tensorboard cuando el backend
# es tensorflow, así que hay que instalar los dos.
keras-tuner==1.4.8
tensorboard==2.21.0
